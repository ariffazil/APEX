{
  "name": "apex-prime",
  "version": "1.0.5",
  "description": "APEX Agent — ASI deliberative server for arifOS AAA federation",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/*.test.js",
    "lint": "echo 'No linter configured — consider adding eslint'",
    "health": "curl -s http://localhost:3002/health | jq ."
  },
  "dependencies": {
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}